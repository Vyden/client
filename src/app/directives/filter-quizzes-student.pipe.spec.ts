import { FilterQuizzesStudentPipe } from './filter-quizzes-student.pipe';

describe('FilterQuizzesStudentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterQuizzesStudentPipe();
    expect(pipe).toBeTruthy();
  });
});
