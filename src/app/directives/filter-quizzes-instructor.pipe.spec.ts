import { FilterQuizzesInstructorPipe } from './filter-quizzes-instructor.pipe';

describe('FilterQuizzesInstructorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterQuizzesInstructorPipe();
    expect(pipe).toBeTruthy();
  });
});
