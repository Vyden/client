import { FilterAnnouncementsPipe } from './filter-announcements.pipe';

describe('FilterAnnouncementsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAnnouncementsPipe();
    expect(pipe).toBeTruthy();
  });
});
