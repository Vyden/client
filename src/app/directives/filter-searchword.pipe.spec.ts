import { FilterSearchwordPipe } from './filter-searchword.pipe';

describe('FilterSearchwordPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSearchwordPipe();
    expect(pipe).toBeTruthy();
  });
});
