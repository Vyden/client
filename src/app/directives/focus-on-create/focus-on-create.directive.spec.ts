import { FocusOnCreateDirective } from './focus-on-create.directive';

describe('FocusOnCreateDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusOnCreateDirective();
    expect(directive).toBeTruthy();
  });
});
