import { FilterForumPipe } from './filter-forum.pipe';

describe('FilterForumPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterForumPipe();
    expect(pipe).toBeTruthy();
  });
});
