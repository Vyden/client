import { FilterQuizResponsesPipe } from './filter-quiz-responses.pipe';

describe('FilterQuizResponsesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterQuizResponsesPipe();
    expect(pipe).toBeTruthy();
  });
});
